namespace ServiceInterface
{
    /// <summary>
    /// Enumeration of all possible user roles.
    /// </summary>
    public enum Role
    {
        Guest,
        RegisteredUser,
        Admin
    }
}